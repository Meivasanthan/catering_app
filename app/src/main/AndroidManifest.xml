<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.catering_app">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_round"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_spalish"
        android:supportsRtl="true"
        android:theme="@style/Theme.CateringApp"
        android:usesCleartextTraffic="true">

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Customer Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ========== OWNER/CATERER ACTIVITIES ========== -->

        <!-- Owner Dashboard Activity -->
        <activity
            android:name=".CatererDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Owner Menu Management Activity -->
        <activity
            android:name=".MenuManagementActivity"
            android:exported="false" />

        <!-- Owner Orders Management Activity -->
        <activity
            android:name=".OwnerOrdersActivity"
            android:exported="false" />

        <!-- Owner Profile Activity -->
        <activity
            android:name=".CatererProfileActivity"
            android:exported="false" />

        <!-- Add Food Activity -->
        <activity
            android:name=".AddFoodActivity"
            android:exported="false" />

        <activity
            android:name=".OrderDetailActivity"
            android:exported="false" />

    </application>

</manifest>